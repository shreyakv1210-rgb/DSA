import pandas as pd

def combine_two_tables(person: pd.DataFrame, address: pd.DataFrame) -> pd.DataFrame:
    result = pd.merge(person,address,how='left',on='personId')
    return result[['firstName','lastName','city','state']]

# Used left join because we need all Person records
# even if some lack Address details.
result = pd.merge(person, address, ...)
